{
    "verbose": false,
    "watch": ["./server"],
    "exec": "rm -rf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json --copy-files && node dist/server/server.js"
}